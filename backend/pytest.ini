[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    error
    # Raised inside starlette's test client against anyio; not our code and not
    # actionable from here.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
